# Todo App с функциональностью поиска

Приложение для управления задачами (todos) с возможностью поиска по тексту задач.

## Функциональность

### Основные возможности

- ✅ Добавление новых задач (появляются вверху списка)
- ✅ Отметка задач как выполненных
- ✅ Удаление задач
- ✅ Очистка всех выполненных задач
- 🔍 **Умный поиск по задачам** (новое!)

### Поиск

- **Мгновенный запуск**: Поиск начинается сразу после ввода первой буквы
- **Умная сортировка**: Наиболее релевантные результаты показываются вверху
- **Быстрый поиск**: Результаты появляются мгновенно без задержек
- **Подсветка результатов**: Найденный текст подсвечивается в задачах
- **Счетчик результатов**: Показывает количество найденных задач
- **Мгновенная очистка**: Кнопка для быстрой очистки поля поиска

## Технологии

- React 18
- TypeScript
- Zustand (управление состоянием)
- SCSS модули
- Vite (сборка)

## Структура проекта

```
src/
├── app/                 # Главный компонент приложения
├── entities/           # Бизнес-логика (todos)
├── features/           # Функциональные модули
│   ├── addTodo/       # Добавление задач
│   ├── clearCompleted/ # Очистка выполненных
│   ├── toggleTodo/    # Переключение статуса
│   └── search/        # 🔍 Умный поиск по задачам
├── shared/            # Общие компоненты и стили
└── widgets/           # Композитные компоненты
```

## Запуск

```bash
# Установка зависимостей
npm install

# Запуск в режиме разработки
npm run dev

# Сборка для продакшена
npm run build
```

## Как работает поиск

1. **Ввод текста**: Пользователь вводит текст в поле поиска
2. **Мгновенный запуск**: Поиск запускается сразу после ввода первой буквы
3. **Умная фильтрация**: Задачи фильтруются по введенному тексту (регистронезависимо)
4. **Сортировка по релевантности**:
   - Приоритет у задач, которые **начинаются** с поискового запроса
   - Затем по **позиции** вхождения запроса в тексте
   - Короткие задачи показываются выше длинных
5. **Подсветка**: Найденный текст подсвечивается в каждой задаче
6. **Результаты**: Показывается количество найденных задач
7. **Быстрый отклик**: Поиск работает мгновенно без задержек

## Особенности реализации

- **Новые задачи вверху**: Только что добавленные задачи появляются в начале списка для лучшей видимости
- **Умная сортировка**: Результаты ранжируются по релевантности для лучшего UX
- **Производительность**: Использование useMemo для оптимизации вычислений
- **UX**: Мгновенный поиск с умной сортировкой и понятной обратной связью
- **Доступность**: Поддержка ARIA-атрибутов и клавиатурной навигации
- **Адаптивность**: Поиск работает совместно с существующими фильтрами (all/active/completed)
